@{
    // Limpiar el localStorage y sessionStorage antes de redirigir
}

<script>
    // Limpiar el localStorage y sessionStorage
    localStorage.clear();
    sessionStorage.clear();

    // Redirigir después de limpiar
    window.location.href = '/Accounts/Login';
</script>
